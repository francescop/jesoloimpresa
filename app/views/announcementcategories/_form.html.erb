<%= form_for(@announcementcategory) do |f| %>
  <% if @announcementcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@announcementcategory.errors.count, "error") %> prohibited this announcementcategory from being saved:</h2>

      <ul>
      <% @announcementcategory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :permalink %><br />
    <%= f.text_field :permalink %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
